<?xml version="1.0" encoding="UTF-8"?>
<!--
  LaunchAgent for the self-hosted Langfuse stack.

  Mirrors the Collector plist: runs `docker compose up -d` at login so the
  Langfuse UI + ingestion endpoint come back automatically after a reboot.
  Compose services already carry `restart: always`, so OrbStack will usually
  resurrect them on its own — this plist is the belt-and-suspenders pass.

  Installation:

    cp infra/langfuse/launchd/io.flume-langfuse.plist \
       ~/Library/LaunchAgents/
    launchctl load -w \
       ~/Library/LaunchAgents/io.flume-langfuse.plist

  Stop / unload:

    launchctl unload \
       ~/Library/LaunchAgents/io.flume-langfuse.plist

  Replace /path/to/flume below with this checkout's absolute path
  before loading the agent. launchd does not expand shell variables here.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>io.flume-langfuse</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/sh</string>
        <string>-c</string>
        <string>docker compose -f infra/langfuse/docker-compose.yml up -d</string>
    </array>

    <key>WorkingDirectory</key>
    <string>/path/to/flume</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin</string>
    </dict>

    <key>RunAtLoad</key>
    <true/>

    <key>StandardOutPath</key>
    <string>/tmp/flume-langfuse.stdout.log</string>
    <key>StandardErrorPath</key>
    <string>/tmp/flume-langfuse.stderr.log</string>
</dict>
</plist>
